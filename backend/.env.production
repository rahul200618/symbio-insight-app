# Production Environment Variables for Backend

# Node Environment
NODE_ENV=production

# Server Configuration
PORT=3002

# Frontend URL for CORS (Update with your production domain)
FRONTEND_URL=https://your-domain.vercel.app

# Database Configuration
# For Vercel deployment, consider using MongoDB Atlas
STORAGE_MODE=atlas
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/symbio?retryWrites=true&w=majority

# For SQLite (not recommended for Vercel due to read-only filesystem)
# STORAGE_MODE=sqlite

# Google Gemini AI API Key
# Get your API key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-min-32-characters-long
JWT_EXPIRE=7d

# Email Service (Optional - for password reset functionality)
# SENDGRID_API_KEY=your_sendgrid_api_key
# EMAIL_FROM=noreply@your-domain.com

# Security (Optional)
# RATE_LIMIT_WINDOW_MS=900000
# RATE_LIMIT_MAX_REQUESTS=100
